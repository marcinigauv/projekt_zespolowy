name: destroy_all

on:
  workflow_dispatch:
    inputs:
      aws_region:
        description: AWS region for the deployment
        required: true
        default: eu-central-1

concurrency:
  group: store-aws-${{ inputs.aws_region }}
  cancel-in-progress: false

jobs:
  destroy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      AWS_REGION: ${{ inputs.aws_region }}
      CDK_DEFAULT_REGION: ${{ inputs.aws_region }}
      CDK_DEFAULT_ACCOUNT: ${{ secrets.AWS_ACCOUNT_ID }}
      STACK_PREFIX: Store

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Validate required CI secrets
        env:
          AWS_ACCOUNT_ID: ${{ secrets.AWS_ACCOUNT_ID }}
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        run: |
          for secret_name in AWS_ACCOUNT_ID AWS_ACCESS_KEY_ID AWS_SECRET_ACCESS_KEY; do
            value="${!secret_name}"
            if [ -z "$value" ]; then
              echo "Missing required secret: $secret_name" >&2
              exit 1
            fi
          done

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ inputs.aws_region }}

      - name: Install CDK dependencies
        working-directory: infra
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          npm install --global aws-cdk

      - name: Destroy stacks
        working-directory: infra
        run: >-
          cdk destroy
          ${STACK_PREFIX}Cdn
          ${STACK_PREFIX}Compute
          ${STACK_PREFIX}Cache
          ${STACK_PREFIX}Containers
          ${STACK_PREFIX}Database
          ${STACK_PREFIX}Network
          --force